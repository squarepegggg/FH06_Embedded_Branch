/*
 * Devicetree overlay for west builds with board `nrf52dk/nrf52832`.
 *
 * This file was missing on branch scanningOnNewBoard, which broke CMake with:
 *   "boards/nrf52dk_nrf52832.overlay: No such file or directory"
 * because an older build cache still referenced this path.
 *
 * The BLE advertising scanner firmware does not require custom hardware
 * nodes here; add pins/sensors under `/` or `&spi1` etc. when you wire them.
 */

/ {
};
